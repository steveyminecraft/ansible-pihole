---
# Pin to fork default branch for a stable role tree (see git history).
roles:
  - name: geerlingguy.docker
    version: "8.0.0"
  - name: pihole
    src: https://github.com/steveyminecraft/docker-pihole
    version: master
