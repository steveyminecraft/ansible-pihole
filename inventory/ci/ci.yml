---
# `ci` is used by playbooks/ci-bootstrap.yaml; `all` still includes these hosts for
# playbooks that target all (syntax-check, bootstrap-pihole, etc.).
all:
  children:
    ci:
      hosts:
        localhost:
          ansible_connection: local
